{
  "name": "mdr-tracker-root",
  "private": true,
  "version": "1.0.0",
  "description": "MDR Vehicle Tracking Workspace",
  "workspaces": [
    "mdr-tracker"
  ],
  "scripts": {
    "build": "npm run build --workspace=mdr-tracker && cp -r mdr-tracker/dist dist"
  }
}
